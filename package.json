{
  "name": "cucumber",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --filter '@turnipxenon/turnip' dev",
    "install:vercel": "git submodule update --init && bun install"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "workspaces": [
    "packages/*"
  ],
  "peerDependencies": {
    "typescript": "^5"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide"
  ]
}
